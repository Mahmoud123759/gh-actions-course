name: 05 - 1 - Event Filters and Activity Types

on: 
 pull_request:
     types:
        - opened
        - synchronize
     branches:
        - main

jobs: 
    echo:
        runs-on: ubuntu-latest
        steps:
            - run: echo "running whenever PR is opened or synchronize and base branch is main"
  